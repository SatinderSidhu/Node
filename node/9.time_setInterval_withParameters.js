// setInterval is used to repeat the job at given interval 

console.log(' Node Started processing JS code line by Line ')

// setInterval is used to repeat the job at given interval 
console.log(' Starting the interval job using SetInterval, it should run a job every 1 second')

function showMe(name)
{console.log(" executing a "+ name + " process");}

setInterval(showMe,1000," complex job ");

    

console.log('Entire java script file has been processed')